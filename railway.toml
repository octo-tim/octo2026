[build]
builder = "NIXPACKS"
buildCommand = "corepack enable && pnpm install --no-frozen-lockfile && rm -rf dist && pnpm build"

[deploy]
startCommand = "pnpm start"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
